Burn follow-ups

- Tighten the `DiagGaussianDistribution` `log_std` shape contract in `crates/r2l-burn-lm/src/distributions/diagonal_distribution.rs`.
  It is typed as `Tensor<B, 2>`, but some construction paths create it from a 1D shape / action-size vector.
  This currently compiles and works with the existing arithmetic, but the convention is loose and should be made explicit later.
